#include "perlin_noise.h"

namespace noises {
namespace nodes
{

    PerlinNoise::PerlinNoise()
    {
        //std::unique_ptr<InputSocket> seed(new InputSocket("Seed"));
        //seed->set_accepts(ConnectionDataType::value<int, 1>());

        //ConnectionDataType::value<int, 1>().is<bool>();
        //inputs_.add(std::move(seed));

    }
} }
